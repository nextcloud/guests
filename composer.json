{
    "require-dev": {
        "jakub-onderka/php-parallel-lint": "^1.0",
        "phpunit/phpunit": "^7"
    },
    "scripts": {
        "lint": "find . -name \\*.php -not -path './vendor/*' -exec php -l \"{}\" \\;"
    }
}
